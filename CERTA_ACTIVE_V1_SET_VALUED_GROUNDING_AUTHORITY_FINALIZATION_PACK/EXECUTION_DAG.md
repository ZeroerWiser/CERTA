# Execution DAG

```text
validate Pack and immutable failed lineage
→ Phase A read-only authority forensic replay
→ GroundingAuthoritySemanticsAuditor
→ Gate A
→ set-valued shadow reconciliation
→ GateCReplayAuditor + HostileLeakageAndHeuristicAuditor
→ Gate Q using unchanged Gate C thresholds
→ conditional minimal implementation
→ all offline tests
→ one ordinary commit/push and source freeze
→ new-root Integration16
→ new-root Constructor64 + live Gate C
→ actual selected-final Decision
→ selected-final prediction close
→ one dev unblind: Gate O + CC/CW/WC/WW
→ PaperClaimBoundaryAuditor
→ conditional blind table-disjoint holdout
→ permanent method freeze
```

No backward edge exists after a scientific Gate failure. No second authority correction is authorized.

Attention budget:

```text
forensic authority analysis and shadow Gate C: 40%
minimal implementation and tests: 20%
Integration16/Constructor64 live validation: 20%
Decision/unblind/holdout and claim audit: 20%
```
