# Matched Constructor Protocol

Run frozen dev64 C0_SCHEMA_ONLY, C1_ROLE_ONLY and C2_ROLE_RETRIEVAL with identical B0, sample order, Role records, model/sampling, Planner, full validator, closure, executor, operation registry and answer equivalence. C2 alone receives frozen retrieval references.

Gate C is recomputed from raw artifacts. `PerSignatureConstructorAuditor` reports real-table constructor, paired executable contrast and registry-complete coverage by signature, operation family, table and failure stage. Synthetic fixtures cannot replace real-table evidence.

Gate C failure permanently freezes the method and returns the stratified evidence.
