# Holdout Protocol

Only Gate O PASS and dev Decision Gate PASS authorize the internal table-disjoint holdout. The method is permanently frozen before holdout.

Run blind B0, frozen Role V3, frozen C2 constructor and frozen Decision once. Holdout selected-final closes before one holdout unblind. No C0/C1 holdout arm and no post-dev or post-holdout method change are allowed.
