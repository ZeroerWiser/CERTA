# CERTA

Minimal, independent CERTA runtime release. It contains no dataset, model,
credentials, run output, historical Git objects, or restricted inputs.

Use `scripts/01_run_main.sh` through `scripts/04_evaluate.sh` with explicit
`CERTA_*` runtime variables. The machine-readable release boundary, checksums,
and parity status are in `audit/`.
