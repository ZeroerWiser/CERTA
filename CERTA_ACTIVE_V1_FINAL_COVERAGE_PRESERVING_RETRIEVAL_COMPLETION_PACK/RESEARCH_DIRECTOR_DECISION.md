# Research Director Decision

```text
AUTHORIZE_CERTA_ACTIVE_V1_FINAL_COVERAGE_PRESERVING_RETRIEVAL_COMPLETION

SUPERSEDE_FREEZE_CERTA_ACTIVE_GROUNDING_AUTHORITY_VALID_NO_PAIRED_CONTRAST
FOR_DESTRUCTIVE_RETRIEVAL_PROJECTION_ONLY
```

The prior Gate C V3 result is scientifically valid: assignment-level grounding authority is accepted and all eleven safety counters are zero. It also demonstrates that the current C2 arm is not a coverage-preserving retrieval augmentation. C1 produced 29 executable derivations and one paired row, while C2 produced six derivations and zero paired rows.

The frozen V3 bridge currently replaces the complete Planner schema with the retrieved header-node subset and deletes every schema edge whose two endpoints are not both retrieved. The retriever supplies at most 64 header node IDs. This creates a hard recall bottleneck and can remove structural connectivity required for finite role alternatives.

Authorize one final method correction: C2 must retain the complete C1 structural reference domain and add retrieval only as proposal-blind focus metadata. Retrieval may guide Planner enumeration but may not authorize, reject, score after execution, or delete structural hypotheses.

This is the last authorized Active V1 method iteration. If unchanged Gate C V3 still fails after the bounded C2-only execution, stop the method line. Do not add another retriever, reranker, agent, score, intervention family, threshold, or Decision policy.
