# CERTA Research Director Decision

```text
AUTHORIZE_CERTA_ACTIVE_V1_ADOPT_PREWARMED_VERIFIED_VLLM_AND_RESUME_FINAL_SCIENTIFIC_DAG

SUPERSEDE_BLOCKED_RUNTIME_READINESS_FAILED_FOR_CONTROLLER_COLD_START_ONLY
```

The previous `BLOCKED_RUNTIME_READINESS_FAILED` remains immutable for its original cold-start lineage. It does not classify the currently prewarmed service, CERTA method, Planner transport projection, Integration16, Constructor, Decision, or scientific DAG.

This is a controller-only runtime adoption. It authorizes no CERTA repository source modification, Role V3 rerun, prompt/schema/Gate/cohort/threshold/eligibility change, second Planner/Constructor/Decision policy, or new scientific component.

```text
method source edits authorized = 0
Role V3 rerun authorized = false
transport projection edits authorized = 0
controller service start/stop/restart/signal authorized = false
scientific POST automatic retries = 0
scientific transport replay authorized = false
```

The user is the runtime operator and retains service-lifecycle authority. Codex may only validate and adopt the exact running process identified by `PREWARMED_VLLM_SERVICE_IDENTITY.json`, run three fresh readiness rounds, write controller state, invoke the frozen preflight, and execute the frozen scientific DAG.

The current service identity expected by this authorization is PID `1349993`, `CUDA_VISIBLE_DEVICES=3`, `127.0.0.1:30338`, served model `Qwen3-8B`, model root `/home/common_data/llm/Qwen/Qwen3-8B`, and `max_model_len=32768`.

All earlier blocked terminals, output roots, logs, checksums, and bundles remain read-only. A new never-existing scientific output root is mandatory.
