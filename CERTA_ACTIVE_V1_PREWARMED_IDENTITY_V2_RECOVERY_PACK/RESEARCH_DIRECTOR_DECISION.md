# CERTA Research Director Decision

```text
AUTHORIZE_CERTA_ACTIVE_V1_PREWARMED_IDENTITY_V2_REFREEZE_AND_FINAL_DAG_RESUME

SUPERSEDE_BLOCKED_PREWARMED_SERVICE_IDENTITY_FAILED_FOR_MIXED_RUNTIME_IDENTITY_LINEAGE_ONLY
```

The blocked adoption emitted zero scientific and transport calls. It does not invalidate the Qwen3-8B service, Role V3, Planner transport projection, Integration16, Constructor, Decision, or any scientific Gate.

The diagnostic establishes:

```text
live PID = 1349993
live GPU = 3
live normalized cmdline SHA256 = df765d9af5cc5aafc518428dd4a545ebee34bfd67a512e9d3f9d8d21ec337977
identity expected cmdline SHA256 = d86efb9407136fc0127fd63e1387632b70f29c0bd5cdf971884fb345413ac3c4
root PID owns port 30338 = true
root or descendant owns port 30338 = true
identity log field = missing
```

The expected `d86efb...` identity originated from a different earlier service lineage. The deployed identity is therefore mixed-lineage and incomplete.

The only authorized recovery is to read the still-running PID 1349993, freeze one complete V2 identity from its current `/proc` and live log descriptors, create a new immutable evidence archive and binding manifest, and adopt that V2 evidence under a new output root.

No service start, stop, restart, signal, reconfiguration, retry, method edit, Role V3 rerun, prompt/schema change, or Gate change is authorized.
